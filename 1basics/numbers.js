//Math. -- contains a lot of helper methods can be used for different math calculation.

//typeof is used to determine the typeof of the data

let randomNum;
randomNum = Math.random() * 11;
console.log(randomNum);

let ann = Math.ceil(randomNum);
console.log(ann);

// checkNumbers = Math.random();
// console.log(checkNumbers);

// breaking the above down

// Math.floor - rounds up a number to the lowest figure

//Math.ceil - rounds up a number to the highest figure

//Math.random  - generates a random number which is always a decimal.

// * indicate the range on the random numbber but not including the number.
